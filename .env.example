# Copia este archivo a `.env` y completa tus valores reales.
# NUNCA subas `.env` a Git: contiene credenciales.
#
# Proyecto Supabase (panel > Project Settings > API):
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_KEY=tu-anon-o-publishable-key
